# APP-290 Plan

1. Add one existing-task-ID helper and exact deferred reason at the Core submit boundary.
2. Apply the same projection to Task Thread, Library, and identity task entry while preserving manual Browser/Harbor controls.
3. Retain only Core-live historical failures and expose owner time with existing provenance/failure fields.
4. Replace positive BOSS smoke admission with zero-request deferred assertions and an XHS non-regression check.
5. Run typecheck, full smoke, packaged smoke, packaged readonly fixture-block smoke, and diff validation before PR creation.
