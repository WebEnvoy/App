# Plan

1. Replace the BOSS blanket block with a narrow structured job-search parser and canonical payload.
2. Align task/skill/identity labels with search-only scope and future real `detail_ref` detail boundary.
3. Add positive and fail-closed smoke cases.
4. Run typecheck, smoke, packaged readonly smoke, and diff checks; publish a ready PR without merge or issue closeout.
5. Keep the identity detail entry aligned with the same per-task read-only admission; unknown and write-precheck entries remain fail closed.
