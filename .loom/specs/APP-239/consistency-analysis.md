# APP-239 Consistency Analysis

- Core owns task/run/result and opaque target truth; App only selects and submits a Core-projected ref.
- Harbor owns browser/session/page/evidence facts; App displays public refs only.
- Lode owns capability metadata; App uses the exact package/task contract consumed by Core.
- No fixture, URL, note ID, xsec, or fallback projection can become a live detail command.
- BOSS remains disabled/deferred and no production request is issued.
